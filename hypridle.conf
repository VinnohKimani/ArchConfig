# Configuring hypridle
# See: https://wiki.hypr.land/Hypr-Ecosystem/hypridle/
# 
# hypridle.conf - Hypridle configuration for HyDE
# Handles idle actions: dim, lock, DPMS, suspend, and custom listeners
# 
# Listeners: Each listener block defines an idle timeout and actions to run
# 
# For more info, see: https://wiki.hypr.land/Hypr-Ecosystem/hypridle/

#!      ░▒▒▒░░░▓▓           ___________
#!    ░░▒▒▒░░░░░▓▓        //___________/
#!   ░░▒▒▒░░░░░▓▓     _   _ _    _ _____
#!   ░░▒▒░░░░░▓▓▓▓▓ | | | | |  | |  __/
#!    ░▒▒░░░░▓▓   ▓▓ | |_| | |_/ /| |___
#!     ░▒▒░░▓▓   ▓▓   \__  |____/ |____/
#!       ░▒▓▓   ▓▓  //____/

$LOCK_CMD = hyde-shell lockscreen.sh # Calls $LOCKSCREEN default is hyprlock
$UNLOCK_CMD = sh -c 'sleep 3 && pkill -9 $(hyde-shell lockscreen --get)' # kills the lockscreen after 3 seconds for cleanup

general {
    lock_cmd = $LOCK_CMD # command when `loginctl lock-session` is called
    unlock_cmd = $UNLOCK_CMD # command when `loginctl unlock-session` is called
    ignore_dbus_inhibit = false             # Ignore dbus idle-inhibit requests (false = respect them)
    ignore_systemd_inhibit = false          # Ignore systemd-inhibit --what=idle (false = respect them)
    before_sleep_cmd = loginctl lock-session && sleep 1 # command ran before sleep
    # after_sleep_cmd = # notify-send "Awake!"  # command ran after sleep
}

# // --- Listener: Dim display after 120s idle ---
listener {
    timeout = 120
    on-timeout = brightnessctl -s && brightnessctl s 1% # Save and set brightness to 1%
    on-resume = brightnessctl -r # Restore brightness on activity
}

# // --- Listener: DPMS off (Screen Sleep) after 300s idle ---
# Turns off display (but not if media is playing)
listener {
    timeout = 300
    on-timeout = hyprctl dispatch dpms off && brightnessctl --device='smc::kbd_backlight' -s && brightnessctl --device='smc::kbd_backlight' set 0% # Turn off display and keyboard backlight
    on-resume = hyprctl dispatch dpms on && brightnessctl --device='smc::kbd_backlight' -r   # Turn display back on and restore keyboard backlight
}

# // --- Listener: Lock after 600s idle ---
# Locks the session after the screen has been sleeping for some time
listener {
    timeout = 600
    on-timeout = loginctl lock-session # Try system lock, fallback to $LOCKSCREEN
}

# // --- Listener: Suspend after 1200s idle ---
listener {
    timeout = 1200
    on-timeout = systemctl suspend # Suspend system
}

# // --- Custom listeners ---
# hyprlang noerror true
# Source any additional listener configs from this path (wildcard supported)
# Note: 'source' command is not implemented yet in hyprlang, but left for future use
source = ./hypridle/*
# hyprlang noerror false
